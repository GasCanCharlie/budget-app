/**
 * PDF Page Extraction — stub
 *
 * We send PDFs directly to Claude via the document API,
 * so per-page text extraction is not needed.
 * This module is kept for potential future deterministic preprocessing.
 */

export {}
